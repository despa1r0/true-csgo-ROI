# trueROI — каталог и сравнение CS2-предметов

Веб-приложение развивается в сторону сравнения цен на CS2-предметы и расчёта реальной прибыли после комиссий. Текущий этап — надёжный локальный каталог скинов, на который позже будут привязаны цены маркетплейсов.

## Что уже работает

1. PostgreSQL в Docker с постоянным volume.
2. Автоматический импорт актуальных скинов из ByMykel/CSGO-API.
3. Поиск по частичному названию с превью.
4. Фильтры по оружию и редкости.
5. Карточка скина и отдельные блоки доступных wear-качеств.
6. Фильтрация обычных, StatTrak™ и Souvenir вариантов.
7. Существующий чистый расчёт прибыли и ROI.

## Модель данных

`skins` хранит базовый скин, например `AK-47 | Redline`. `skin_variants` хранит реальные рыночные варианты, например `StatTrak™ AK-47 | Redline (Field-Tested)`. Связь выполняется по `skin_id` источника.

Рыночное имя, float-диапазон, paint index, редкость, изображение и исходный JSON сохраняются в БД. Это даёт стабильную основу для таблиц цен и истории.

## Следующие этапы

1. Добавить таблицы маркетплейсов, текущих цен и времени обновления.
2. Подключать каждый внешний API через отдельный adapter.
3. Связать цены с `skin_variants.market_hash_name`.
4. Вернуть в новый интерфейс сравнение цен и Profit Engine.
5. Добавить ликвидность, историю цен и автоматический поиск возможностей.

## Формула прибыли

```text
effective_buy = buy_price + buy_fees
effective_payout = sell_price - sell_fees
profit = effective_payout - effective_buy
roi = profit / effective_buy * 100
```

Деньги передаются и хранятся в центах, а не в `float`. Profit Engine не выполняет HTTP-запросы и работает только с нормализованными ценами и комиссиями.
